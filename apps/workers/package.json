{
  "name": "workers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsbuild": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js ",
    "dev": "node dist/index.js",
    "start": "pnpm tsbuild && pnpm dev",
    "build": "pnpm tsbuild && pnpm dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/ws": "^8.18.1",
    "bullmq": "^5.56.0",
    "esbuild": "^0.25.5",
    "ws": "^8.18.3"
  }
}
